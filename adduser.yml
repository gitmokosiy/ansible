---

# Generate password with: 
# python -c 'import crypt; print crypt.crypt("This is my Password", "$1$SomeSalt$")'

- name: User Create
  hosts: all

  tasks:
    - name: Creating user 
      user: 
        name={{user}}
        password={{password}}
        groups={{groups}} 

